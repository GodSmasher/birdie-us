{
  "name": "@birdie/netzbot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Portal-Bot für die Netzanmeldung — loggt sich beim Netzbetreiber ein, füllt das Online-Formular vor und speichert es als Entwurf (Human-in-the-loop). Läuft als eigener Worker, NICHT auf Vercel.",
  "scripts": {
    "dev": "tsx src/runner.ts",
    "once": "tsx src/runner.ts --once",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
