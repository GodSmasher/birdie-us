# .birdie

Internal automation platform — connect tools, run bots, control everything from one dashboard.

Demo deployment of the visual frontend. No backend, no auth — mock data only.

## Local

```
npm install
npm run dev
```
